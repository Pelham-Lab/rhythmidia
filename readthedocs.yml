# readthedocs.yml

version: 2

build:
  #image: latest
  os: "ubuntu-22.04"
  tools:
    python: "3.11"

python:
  version: "3.11"
  install:
    - method: pip
      path: .

conda:
  environment: docs/requirements.yaml